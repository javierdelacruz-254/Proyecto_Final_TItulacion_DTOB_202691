import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Colores principales
  static const primary = Color(0xFF6EC1E4); // Azul calmante
  static const secondary = Color.fromARGB(
    255,
    223,
    164,
    163,
  ); // Rosa suave, confort

  // Variaciones del color principal
  static const primaryLight = Color(0xFFB3E0F7); // Azul pastel más claro
  static const primaryDark = Color(0xFF3B9ACC); // Azul más intenso

  // Neutros
  static const background = Color(0xFFFFFBF7); // Crema suave
  static const surface = Color(
    0xFFFFFFFF,
  ); // Blanco puro para cards o superficies

  // Texto
  static const textPrimary = Color(
    0xFF333333,
  ); // Gris oscuro, buena legibilidad
  static const textSecondary = Color(0xFF666666); // Gris medio

  // Estados
  static const success = Color(0xFF4CAF50); // Verde confianza, éxito
  static const error = Color(0xFFF44336); // Rojo alerta
  static const warning = Color(0xFFFFC107); // Amarillo suave para advertencias
  static const info = Color(0xFFA3D5D3);
}

class AppColorsCalma {
  AppColorsCalma._();

  static const primary = Color(0xFF6EC1E4);
  static const secondary = Color(0xFFF9AFAE);
  static const primaryLight = Color(0xFFB3E0F7);
  static const primaryDark = Color(0xFF3B9ACC);
  static const background = Color(0xFFFFFBF7);
  static const surface = Color(0xFFFFFFFF);
  static const textPrimary = Color(0xFF333333);
  static const textSecondary = Color(0xFF666666);
}

class AppColorsEnergia {
  AppColorsEnergia._();

  static const primary = Color(0xFFFF6F61);
  static const secondary = Color(0xFFFFD670);
  static const primaryLight = Color(0xFFFFA88A);
  static const primaryDark = Color(0xFFC7544D);
  static const background = Color(0xFFFFF8F0);
  static const surface = Color(0xFFFFFFFF);
  static const textPrimary = Color(0xFF2E2E2E);
  static const textSecondary = Color(0xFF5A5A5A);
}

class AppColorsElegancia {
  AppColorsElegancia._();

  static const primary = Color(0xFFB5838D);
  static const secondary = Color(0xFFF2D7D5);
  static const primaryLight = Color(0xFFD9AFC5);
  static const primaryDark = Color(0xFF8C4B55);
  static const background = Color(0xFFFFF5F5);
  static const surface = Color(0xFFFFFFFF);
  static const textPrimary = Color(0xFF3A3A3A);
  static const textSecondary = Color(0xFF6E6E6E);
}
